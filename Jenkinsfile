pipeline {
    agent any

    stages {
        stage('Checkout GIT') {
            steps {
                echo 'pulling..'
                git branch: 'main',
                url : 'https://github.com/mohamedachech/devops.git'
            }
        stage('sehli') {
            steps {
                echo 'sehli 7awi'
            }
        }
    }
}
}
